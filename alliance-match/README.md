# 同盟 3人組マッチング (alliance-match)

ゲームの **投票メンバー画面のスクリーンショット** をアップロードすると、名前・戦力(グーパンチ横の数値)・開催日時を読み取り、
戦力の高いメンバーを各組に1人ずつ配置した **3人組** を自動で編成して、メンバー向けの **お知らせ文** を作る Web アプリです。

- サーバー不要の静的サイト(GitHub Pages で公開できます)
- 文字認識(OCR)は [Tesseract.js](https://github.com/naptha/tesseract.js) でブラウザ内で実行。画像は外部に送信されません
- 管理者が結果をコピーして、同盟チャットやメールにそのまま貼り付けられます

## 使い方

1. **スクショを選ぶ**  
   - **投票メンバー**画面(複数枚OK): 名前と戦力(グーパンチ横の数値)を読み取ります。同じ人は1回だけ数えます。
   - **同盟投票**画面(1枚): 「【選択】24日(木)22:30から」のような文から開催日時を読み取ります。
2. **読み取り結果を確認する**  
   化けた名前や戦力は表で直せます。名前は「名前の修正」タブでまとめて直すこともでき、一度修正したら学習して次回から正しく読み取ります。参加しない人は ✕ で外せます。
3. **組分けを作る**  
   戦力の近い人同士で組みます。戦力順に並べて上から3人ずつ区切るので、**A が最も強いチーム**になり、以下 B、C… と続きます。
   人数が3で割り切れないときは、2人組は作らず、余った人を弱い側の組に加えて4人組にします(10人なら 3・3・4、11人なら 3・4・4)。
4. **お知らせ文をコピーする**  
   イベント名・開催日時・本部リーダー(いちばん戦力の高い人。変更可)を差し込んだお知らせ文ができます。
   文面のひな形とイベント名は「お知らせ文編集」タブで直して「保存」します。「戦力も載せる」で各組の戦力と合計を添えられます。

既定のお知らせ文:

```
【クレイジージョイ】チーム発表

次回の開催日時:9/24(木)22:30

【A】nnnnnnn ・ ayataka3 ・ むにお
【B】黒豆禿げ茶 ・ chucky ・ ふくにゃんBLv
【C】アスカーニャ ・ un temps libre ・ てとてと ・ Hunter

【本部リーダー】nnnnnnn

※事前準備のため、10分前にはオンラインになって下さい。
不明点があれば同盟チャットで確認してください。
よろしくお願いします！
```

### ひな形と学習の保存先

サーバーが無いので、ひな形と名前の学習は端末(ブラウザ)ごとに保存されます。
`data/template.json` を更新して `version` を上げると、各端末は次回アクセス時にそのひな形を取り込みます(全員への配布用)。

### 名前の修正(学習)

「名前の修正」タブに、今回読み取った名前の一覧が出ます。右の欄に正しい名前を入れて「修正完了」を押すと学習し、次回から正しく読み取ります。
マッチング画面の表で名前を直した場合も同じように学習します。

- 内部では、同盟メンバーの正しい名前の一覧(`data/names.json` に同梱)と、読み取り → 正しい名前の読み替え辞書を端末(ブラウザ)に保存しています。
- 読み取った名前が辞書にあれば確定、登録名に十分似ていれば(類似度 0.6 以上で候補が1つ)自動でその名前に寄せます。
- `data/names.json` の `seedVersion` を上げると、各端末は次回アクセス時に新しい名前を自動で追記します(端末側の学習は残ります)。

### 画面下で切れたカード

投票メンバー画面の最下段でカードが途中まで見えている場合、名前と戦力が見えていれば読み取ります(半分以上見えていることが目安)。

### 開催日時の読み取り

同盟投票画面の文から `24日(木)22:30`、`9/24(木) 22:30`、`9月24日 22:30` のような表記を探します。
月が無い表記は、今日以降で最初に来る「その日で曜日が合う月」を自動で補います(9月25日以降に「24日(木)」なら 12/24(木))。
読み取れなかったときや違っていたときは、お知らせ文の「開催日時」欄で直してください。

## 公開方法(GitHub Pages)

1. このリポジトリを GitHub に push します。
2. リポジトリの **Settings → Pages** で Source を `Deploy from a branch`、Branch を `main` / `/ (root)` にして保存。
3. 数分後に `https://<ユーザー名>.github.io/<リポジトリ名>/alliance-match/` で開けます。

初回アクセス時は Tesseract.js 本体と日本語・英語の言語データ(合計 10MB 弱)を jsDelivr CDN から取得します。
2回目以降はブラウザにキャッシュされます。

## claude.ai の Artifact として公開する

GitHub Pages を使わずに、claude.ai 上のページとして動かすこともできます。Artifact では外部CDNからの実行時取得ができないため、
Tesseract の本体・ワーカー・言語データを同梱した配布物を作ります。

```bash
npm run vendor            # Tesseract 資材を ./vendor に用意
npm run build:artifact    # ./dist-artifact に公開用ファイル一式を生成
```

`dist-artifact/index.html` と `js/`, `vendor/` を Artifact として公開します(Claude Code から `Artifact` ツールで公開できます)。
言語データは配信できる拡張子の都合で `.traineddata.wasm` という名前にし、ページ側で取得してワーカーへ渡します。

## 読み取りのコツ

- スクショは端末のスクリーンショット機能で撮った元サイズのまま使ってください(圧縮・トリミングすると精度が落ちます)。
- 画面の下で途中まで切れているカードも、名前と戦力が見えていれば読み取ります。名前や戦力まで隠れている人はスクロールしてもう1枚撮ってください。
- 日本語の名前は読み違いが出ることがあります。信頼度の低い行は黄色で表示されるので、表で直してからお知らせ文を作ってください。

## 開発

```bash
cd alliance-match
npm test                       # 編成・日時解釈・お知らせ文の単体テスト(Node 20+)
npm run vendor                 # オフライン/ローカルテスト用に Tesseract 資材を ./vendor へ配置
npm run serve                  # http://127.0.0.1:8765 で配信(?ocr=local を付けると vendor を使用)
npm i && FIXTURES=/path/to/shots npm run e2e   # Chromium でスクショを流す実機テスト
```

構成:

- `index.html`, `css/style.css` … 画面
- `js/ocr.js` … カードの検出、前処理、Tesseract 呼び出し、開催日時の検出(日本語のみの散在テキストモード)
- `js/matching.js` … 戦力の解釈、組分け、日時の解釈、名前の読み替え、お知らせ文の生成(純粋関数)
- `js/names.js` … 名前マスターの保存/読込/JSON入出力
- `data/names.json` … 名前マスターの初期データ
- `js/app.js` … 画面制御
